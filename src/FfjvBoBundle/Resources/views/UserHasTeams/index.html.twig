{% extends('@FfjvBo/layout.html.twig') %}

{% block content %}
    <h1>UserHasTeams list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Roles</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('userhasteams_show', { 'id': entity.id }) }}">{{ entity.roles }}</a></td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('userhasteams_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('userhasteams_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('userhasteams_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
