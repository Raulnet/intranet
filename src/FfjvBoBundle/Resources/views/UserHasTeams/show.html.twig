{% extends('@FfjvBo/layout.html.twig') %}

{% block content %}
    <h1>UserHasTeams</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Roles</th>
                <td>{{ entity.roles }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('userhasteams') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('userhasteams_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
